{
  "name": "alumni-homecoming-cron",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "prisma generate && tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "ts-node --transpile-only src/index.ts"
  },
  "dependencies": {
    "@prisma/client": "^5.15.0",
    "node-cron": "^3.0.3"
  },
  "devDependencies": {
    "@types/node": "^20.14.9",
    "@types/node-cron": "^3.0.11",
    "prisma": "^5.15.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.5.3"
  }
}
